This is an exceptional prompt. It’s structured, constrained, and demands a specific, high-value output. You are effectively programming the AI to bypass generalities and act as a Senior CTO.
Here is the Twin Content Operating System blueprint, executed exactly as requested.
1. PRODUCT NAME & TAGLINE
 * Name: Twin
 * Tagline: Your Digital Self. Only Better.
2. CORE FEATURES
 * Voice Cloning Engine: Analyzes past content to build a vector-based style profile (syntax, slang, cadence) that makes AI indistinguishable from you.
 * Voice-to-Thread: Transforms 60-second rambling voice notes into structured, high-hook Twitter threads instantly.
 * Viral Hook Library + A/B: Auto-generates 5 distinct opening hooks per thread based on 200+ viral templates.
 * Trend-Jacker: Ingests live trends (X/Google/DexScreener) and rewrites your core topic to ride the current wave.
 * Smart Remix: "Steal Like an Artist" mode that strips the formatting/pacing of a viral hit and fills it with your topic.
 * Cringe-Check: A dedicated sentiment filter that removes corporate jargon, excessive emojis, and "ChatGPT vibes."
 * Auto-Image/Meme Gen: Text-to-image pipeline (Flux/Ideogram) using your brand color palette and specific humor style.
 * Reply Guy Drafter: Monitors notifications and drafts 10 context-aware, spicy replies for one-click approval.
 * Performance Feedback Loop: Analyzes your specific winners (e.g., "threads with numbers in the first tweet") and coaches you on the next post.
 * Agency Dashboard: Multi-tenant architecture to manage infinite client "voices" from a single login.
 * Omni-Repurpose: Instantly converts a successful thread into a LinkedIn Carousel (PDF), Newsletter (HTML), and Script.
 * Smart Scheduler: Queue-based system with rate-limit protection and "Best Time to Post" prediction.
3. DATABASE SCHEMA (Supabase / PostgreSQL)
-- Enable Vector Extension for AI Memory
create extension if not exists vector;

-- 1. Users (Auth handled by Supabase Auth/Clerk, this extends it)
create table users (
  id uuid references auth.users primary_key,
  username text,
  subscription_tier text default 'free', -- free, pro, agency
  brand_colors jsonb, -- { primary: '#FF0000', secondary: '#000000' }
  created_at timestamptz default now()
);

-- 2. Voice Packs (The "Twin" Brain)
create table voice_packs (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid references users(id),
  name text, -- e.g., "Savage Mode", "Educational"
  description text,
  base_prompt text, -- The system prompt generated by analysis
  writing_samples text[], -- Raw text used for tuning
  embedding vector(1536) -- For RAG retrieval of style
);

-- 3. Threads (The Content)
create table threads (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid references users(id),
  voice_pack_id uuid references voice_packs(id),
  topic text,
  status text, -- draft, scheduled, posted
  content jsonb, -- Array of tweet objects: [{text: "", media: ""}, ...]
  scheduled_for timestamptz,
  posted_at timestamptz,
  platform_id text -- Twitter Thread ID after posting
);

-- 4. Analytics (The Feedback Loop)
create table analytics (
  id uuid primary key default uuid_generate_v4(),
  thread_id uuid references threads(id),
  impressions int,
  likes int,
  replies int,
  retweets int,
  profile_clicks int,
  fetched_at timestamptz default now()
);

-- 5. Hooks / Templates (Global & User Specific)
create table hooks (
  id uuid primary key,
  category text, -- crypto, tech, life advice
  template_text text, -- "I spent [X] hours doing [Y]. Here's what I learned:"
  is_premium boolean default false
);

4. FOLDER STRUCTURE (Next.js 15 App Router)
/app
  /api              # API Routes
    /generate       # AI Generation endpoints
    /webhooks       # Stripe & Twitter webhooks
    /cron           # Scheduling workers
  /dashboard        # Protected User Area
    /create         # Thread Editor
    /voice          # Voice Pack Training
    /calendar       # Scheduler View
    /analytics      # Stats Dashboard
    /settings       # Account & Billing
  /login            # Auth pages
  page.tsx          # Landing Page
  layout.tsx        # Root Layout
/components
  /editor           # ThreadComposer, TweetPreview
  /ui               # Shadcn UI (Button, Card, Slider)
  /visuals          # ImageGenerator, ChartRenderer
/lib
  ai.ts             # Vercel AI SDK config (OpenAI/Claude)
  supabase.ts       # DB Client
  twitter.ts        # Twitter API Client
  utils.ts          # Helper functions
/actions            # Server Actions (Mutations)
  generate-thread.ts
  schedule-post.ts
  clone-voice.ts

5. MASTER SYSTEM PROMPTS (The Brain)
1. VOICE CLONING ARCHITECT
> "You are an expert linguist and ghostwriter. Analyze the following 20 tweets provided by the user. Deconstruct their writing DNA into a structured 'Voice Profile'. Identify: 1) Sentence length variance. 2) Preferred slang/idioms (e.g., 'bruh', 'ngmi'). 3) Formatting habits (bullet points, arrows, whitespace). 4) Emotional tone (sarcastic, stoic, hype). Output a System Prompt that tells another AI exactly how to write like this person. Do not summarize the content; summarize the style."
> 
2. THE THREAD WRITER (CORE)
> "You are Twin, a specialized ghostwriter. Your goal is to write a viral Twitter thread about [TOPIC].
> VOICE SETTINGS: Use the following Voice Profile: [INSERT VOICE PROFILE].
> RULES:
>  * No corporate fluff. No 'In conclusion'.
>  * Use the 'Georgia Coast Swagger': Laid back, confident, use 'king', 'fam', or 'wild' where appropriate.
>  * Structure: Tweet 1 is the Hook (Create a curiosity gap). Tweet 2-N provides value/story. Last Tweet is the CTA.
>  * Keep tweets under 250 chars for readability.
>  * Use engaging formatting (short lines, aggressive spacing)."
> 
3. VOICE-TO-THREAD (TRANSCRIPTION)
> "Take this messy, rambling transcript of a voice note. Extract the core value proposition. Reassemble it into a cohesive Twitter thread using the User's Voice Profile. Cut the stuttering, keep the energy. If the user sounds excited in the audio, use uppercase words for emphasis. If they sound serious, keep it punchy and short."
> 
4. THE VIRAL HOOK GENERATOR
> "Generate 5 distinct opening tweets (hooks) for a thread about [TOPIC].
> Variants:
>  * The 'Negative' Frame (Stop doing X).
>  * The 'Numbers' Frame (How I made $X in Y days).
>  * The 'Story' Frame (I lost everything, then this happened).
>  * The 'Contrarian' Frame (Everyone is lying to you about X).
>  * The 'List' Frame (7 tools to save your life).
>    Ensure all hooks sound like the user's defined Voice Profile."
> 
5. THE "CRINGE-CHECK" FILTER
> "Review the following thread draft. Score it from 0-100 on the 'Cringe Scale'.
> Flags to remove: Hashtag stuffing, 'Delve', 'Unlock', 'Game-changer', 'Rocket emojis', Generic motivation.
> If Score > 20: Rewrite the flagged sections to sound more human, cynical, and grounded. Make it sound like it was written at 2 AM with a glass of whiskey, not by a PR intern."
> 
6. IMAGE PROMPT GEN (FLUX/SD3)
> "Create a stable diffusion prompt for an image to accompany a tweet about [TOPIC].
> Style: Minimalist vector art OR Gritty meme style (depending on user pref).
> Colors: Use [USER_BRAND_HEX] as the primary accent.
> Subject: Visual metaphor. If topic is 'speed', show a cheetah on a rocket, not a stopwatch. No text inside the image."
> 
7. REPLY GUY DRAFTER
> "You are a community manager. Read this incoming tweet: '[TWEET_TEXT]'.
> Draft 3 replies:
>  * Supportive/Hype (High energy).
>  * Question/Engage (Ask a follow-up).
>  * Witty/Slightly Roast (Playful banter).
>    Keep all under 140 chars. No generic 'Great post!' comments."
> 
8. REMIX / STEAL STRUCTURE
> "Analyze this viral thread provided by the user. Strip away the specific topic/content. Keep the skeleton (e.g., Hook -> Problem -> Agitation -> Solution -> Evidence -> CTA).
> Now, write a new thread using that exact skeleton, but about [USER_NEW_TOPIC]. Keep the pacing identical."
> 
9. TREND-JACKING BLEND
> "Current Trend: [TRENDING_TOPIC] (e.g., 'Apple Vision Pro').
> User's Niche: [USER_NICHE] (e.g., 'Real Estate').
> Task: Write a thread bridging these two. Example: 'How the Apple Vision Pro changes Real Estate tours forever.' Make it timely and urgent."
> 
10. REPURPOSING (LINKEDIN)
> "Convert this Twitter thread into a LinkedIn post.
> Changes: Remove the 'thread' numbering. Combine short lines into cohesive paragraphs. Make the tone slightly more professional but keep the 'swagger'. Add a 'P.S.' at the bottom."
> 
11. FEEDBACK LOOP COACH
> "Analyze these last 10 threads. Which ones had the highest engagement? Identify the pattern (e.g., 'Did we use a question in the hook?', 'Did we use an image?').
> Output a concise coaching tip: 'Hey King, your threads with memes are performing 2x better. Let's create a meme for the next one.'"
> 
12. MEME MAKER
> "Take the core concept of this tweet: '[TWEET_TEXT]'. Suggest a popular meme format (e.g., 'Distracted Boyfriend', 'Midwit Bell Curve') that fits perfectly. Write the top text and bottom text for that meme."
> 
13. "WILD CARD" (CHAOS MODE)
> "Rewrite this thread to be 50% more controversial. Take a strong stance. Make people angry enough to comment. Use absolute language ('Always', 'Never')."
> 
14. BIO OPTIMIZER
> "Analyze the user's current Twitter Bio. Rewrite it to convert profile visits into followers. Formula: Who you are + What you build/do + Social Proof + CTA. Remove fluff."
> 
15. AUTO-DM SALES
> "Draft a DM to send to people who commented 'SEND' on the last thread. Tone: Helpful, low pressure. 'Yo, here's that link you asked for. Let me know if you have qs.'"
> 
6. API ROUTES (Next.js)
 * POST /api/auth/twitter (Handle OAuth)
 * POST /api/voice/clone (Input: text samples -> Output: Voice ID)
 * POST /api/generate/thread (Input: Topic + Voice ID -> Output: JSON Thread)
 * POST /api/generate/image (Input: Context -> Output: Image URL)
 * POST /api/generate/remix (Input: Ref URL + Topic -> Output: JSON Thread)
 * GET /api/schedule (Fetch upcoming posts)
 * POST /api/schedule/queue (Add to queue)
 * POST /api/webhooks/twitter (Listen for replies/mentions - Requires Enterprise API usually, stick to polling for MVP)
7. FRONTEND PAGE & COMPONENT LIST
Pages:
 * dashboard/page.tsx: Overview, "Coach" tips, Quick actions.
 * dashboard/create/page.tsx: The "Twin" Editor. Left side: Inputs/Voice. Right side: Live Preview.
 * dashboard/voice/page.tsx: Upload samples, tune sliders (Sarcasm, Length, Emoji use).
 * dashboard/calendar/page.tsx: Drag-and-drop calendar view.
Components:
 * ThreadComposer.tsx: The main writing interface.
 * TweetPreview.tsx: Pixel-perfect mobile rendering of a tweet.
 * VoiceSelector.tsx: Dropdown to switch between "Personal", "Sales", "Shitposting" modes.
 * MagicButton.tsx: The "Generate" button with sparkles.
 * CringeMeter.tsx: Visual gauge for the cringe-check.
8. SCHEDULING & POSTING STRATEGY (Safety First)
 * API Limits: Twitter v2 Basic tier is limited (approx 100 posts/day/user cap implies careful management).
 * The "Human" Delay: Do not blast a thread of 10 tweets instantly. Code a 45-90 second variable delay between threaded tweets to mimic typing.
 * Token Management: Store OAuth tokens securely in Supabase (encrypted). Handle refresh tokens automatically.
 * Safety Switch: If X API returns 429 (Too Many Requests), immediately pause the queue for that user for 15 minutes.
9. PRICING TIERS & LAUNCH PLAN
Tiers:
 * Starter ($29/mo): 1 Voice, Manual Posting, Basic Generation.
 * Growth ($49/mo): 3 Voices, Auto-Scheduling, Image Gen, Analytics.
 * Agency ($199/mo): 10 Client Accounts, Unlimited Voices, Whitelabel Reports.
Path to $50k MRR:
 * Launch: Lifetime Deal (LTD) on AppSumo/ProductHunt for first 200 users ($149 one-time). Cash injection to fund API costs.
 * Growth: Affiliate program (30% recurring) for Twitter influencers.
 * Scale: Focus entirely on the Agency tier. 250 Agencies = $50k MRR.
10. 30-DAY MVP ROADMAP
 * Days 1-5: Setup. Next.js repo, Supabase Auth & DB, Twitter Developer Account approval.
 * Days 6-12: The Brain. Build the Voice Cloning pipeline (OpenAI API) and the Thread Generator.
 * Days 13-18: The Interface. Build the Editor and Live Preview. Connect the "Generate" button.
 * Days 19-24: The Logistics. Build the Scheduler (Cron jobs) and Image Gen integration.
 * Days 25-28: Polish. Mobile responsiveness, Cringe-Check tuning, payment integration (Stripe).
 * Days 29-30: Ship. Internal testing, record demo video, launch on X.
